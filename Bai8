using System;

class Program
{
    static double TinhTong(int n)
    {
        double sum = 0;
        for (int i = 1; i <= n; i++)
        {
            sum += (double)(2 * i + 1) / (2 * i + 2);
        }
        return sum;
    }
}
